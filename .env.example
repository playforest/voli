# Options Query Engine — example .env.
#
# Copy to `.env` (gitignored). Loaded by the CLI on startup via python-dotenv.
# Anything you set here wins over config.yaml.

# ----- secrets ----------------------------------------------------------------

# Required for live Polygon queries.
POLYGON_API_KEY=replace_me

# ----- optional Polygon settings ----------------------------------------------

# Print every Polygon HTTP request/response. Useful when something hangs.
# POLYGON_HTTP_DEBUG=1

# ----- caching / traces -------------------------------------------------------

# OQE_CACHE_PATH=~/.oqe/cache.sqlite
# OQE_TRACE_DIR=~/.oqe/traces

# ----- CLI presentation -------------------------------------------------------

# OQE_THEME=bloomberg
# OQE_THEME_CURSOR=~/.oqe/theme_cursor

# Disable colour output entirely.
# NO_COLOR=1

# ----- logging ----------------------------------------------------------------

# OQE_LOG_LEVEL=INFO
# OQE_LOG_FORMAT=text
